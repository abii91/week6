# For running node server

1) Run => npm install
2) Run => node app.js

# For running angular app

1) Open new terminal
2) Run => cd views
3) Run => npm install
4) Run => ng serve
6) Visit => http://localhost:4200
